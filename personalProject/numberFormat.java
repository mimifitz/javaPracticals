import java.numberFormat;
import java.text.NumberFormat;
import java.util.Scanner;

public class numberFormat {
    public static void main(String[] args) {
        // FORMATTING NUMBERS
        // NumberFormat currency = NumberFormat.getCurrencyInstance();
        // String result = currency.format(66778.89);
        // System.out.println(result); //prints 66,778.89 euros

        // NumberFormat percent = NumberFormat.getPercentInstance();
        // String result = percent.format(5.9);
        // System.out.println(result); // prints 590%
        // OR
        // String result = NumberFormat.getPercentInstance().format(5.9);
        // System.out.println(result); // prints 590%

        // READING USER INPUT (MUST INPORT java.util.Scanner)
        // Scanner scanner = new Scanner(System.in);// read data from terminal window
        // with system.in
        // byte age = scanner.nextByte();
        // System.out.println("What's your age" + age);

        // Scanner scanner = new Scanner(System.in);// read data from terminal window
        // with system.in
        // System.out.print("Phone number: "); // input by user
        // long phoneNumber = scanner.nextLong();
        // System.out.println("What's your phone number: " + phoneNumber);

        /// calling a STRING
        // Scanner scanner = new Scanner(System.in);// read data from terminal window
        /// with system.in
        // System.out.print("Name: "); // input by user
        // String name = scanner.nextLine().trim(); // chainng multiple methods
        // System.out.println("Your name is " + name);
    }
}
